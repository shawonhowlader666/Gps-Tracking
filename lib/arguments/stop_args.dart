import 'package:gpspro/services/model/playback_route.dart';

class StopArguments {
  final int id;
  final String name;
  final PlayBackRoute route;
  StopArguments(this.id, this.name, this.route);
}
